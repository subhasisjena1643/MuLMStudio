# µLM Studio — Python backend dependencies
# Pin PyTorch 2.3.x as specified in the prototype plan.
# Install BEFORE any other package to avoid version conflicts.

# ── Core ML ────────────────────────────────────────────────────────────────────
torch==2.3.1
torchvision==0.18.1          # needed alongside torch for consistent CUDA builds

# ── Backend / API (for later, when you build the FastAPI server) ───────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
websockets==12.0

# ── Utilities ──────────────────────────────────────────────────────────────────
pydantic==2.7.1              # FastAPI's data validation layer
